# Habit Tracker (Python & SQLite)

A lightweight, terminal-based application designed to track daily habits using a persistent SQLite database. This project demonstrates backend logic, data persistence, and CRUD operations.

## 🚀 Features
* **Persistent Storage:** Uses SQLite to ensure data is saved between sessions.
* **Automatic Setup:** Automatically creates the database and tables on the first run.
* **CRUD Operations:** Create, Read, Update, and Delete habits through the terminal.
* **Progress Tracking:** View history and consistency for specific habits.

## 🛠️ Tech Stack
* **Language:** Python 3.x
* **Database:** SQLite3
* **Environment:** Linux / Terminal
